{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/backend/*"
  ],
  "scripts": {
    "start:dev": "lerna run --parallel start:dev"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.2.0"
  }
}
